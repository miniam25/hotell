git init
git config --global user.name "Иван Иванов"
git config --global user.email ivanov@example.com
git commit -m "Реализован механизм аутентификации пользователей"
git commit -m "Исправлена ошибка двойного бронирования"
